syntax = "proto2";

service WalletRoutes{
    rpc GetBalance(Wallet) returns (Wallet) {}
    // rpc GeneratePaymentOrder(PaymentOrder) returns (){}
}

message Wallet {
    required string id = 1;
    optional string total_balance = 2;
}